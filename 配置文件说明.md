# Claude Code 配置文件清单

本仓库已为 AI 编程助手（特别是 Claude Code）配置了以下文件：

## 配置文件列表

### 1. Claude Code 专用配置

#### `.claude/settings.json`
- **用途**：Claude Code 的主配置文件
- **功能**：
  - 定义文件访问权限
  - 设置命令执行规则
  - 保护敏感文件（`.env`、密钥、凭证）
  - 指定需要确认的操作（如安装依赖）
  - 设置上下文忽略模式以提高性能

#### `CLAUDE.md`
- **用途**：项目级别的 Claude Code 指令
- **功能**：
  - 提供项目特定的开发指南
  - 定义代码标准和测试要求
  - 设置安全要求
  - 说明工作流程

#### `.claudeignore`
- **用途**：排除不需要 Claude Code 分析的文件
- **功能**：
  - 忽略依赖目录（node_modules、vendor）
  - 忽略构建输出（dist、build）
  - 忽略日志文件
  - 提高 AI 分析性能

### 2. GitHub Copilot 配置

#### `.github/copilot-instructions.md`
- **用途**：GitHub Copilot 的仓库级指令
- **功能**：
  - 定义代码生成标准
  - 设置边界以防止不需要的更改
  - 指定安全和测试要求
  - 提供项目上下文以获得更好的建议

### 3. Cursor AI 配置

#### `.cursorrules`
- **用途**：Cursor AI 的规则和指南
- **功能**：
  - 建立编码原则
  - 定义代码风格指南
  - 设置安全和性能期望
  - 指导重构和测试实践

### 4. 通用配置

#### `.editorconfig`
- **用途**：跨编辑器的代码格式配置
- **功能**：
  - 确保所有编辑器的一致格式
  - 定义缩进、行尾和字符编码
  - 支持所有主流 IDE 和文本编辑器

#### `.gitignore`
- **用途**：Git 版本控制忽略规则
- **功能**：
  - 排除依赖目录
  - 排除构建产物
  - 排除环境变量文件
  - 排除临时文件和日志

### 5. 文档文件

#### `README.md`
- **用途**：项目主文档
- **功能**：
  - 说明所有 AI 配置文件的用途
  - 提供入门指南
  - 解释安全保护措施
  - 介绍开发工作流

#### `CONTRIBUTING.md`
- **用途**：贡献指南
- **功能**：
  - 说明如何使用 AI 编程助手贡献代码
  - 定义代码标准
  - 设置安全要求
  - 提供贡献流程

## 文件保护

配置已设置以下安全保护：

### 禁止访问的文件
- 环境变量文件：`**/.env*`
- 私钥文件：`**/*.pem`、`**/*.key`
- 凭证目录：`**/secrets/**`、`**/credentials/**`

### 需要确认的操作
- 安装依赖：`npm install`、`yarn add`
- 修改 package.json
- 删除文件

### 禁止的命令
- `rm -rf *`（强制递归删除）
- `sudo *`（超级用户命令）

## 使用方法

1. 克隆仓库后，AI 编程助手会自动检测这些配置文件
2. 助手将遵循定义的指南来帮助您编写代码
3. 配置会保护敏感文件并防止危险操作
4. 所有 AI 助手（Claude Code、GitHub Copilot、Cursor）都将受益于这些配置

## 适用的 AI 工具

- ✅ Claude Code
- ✅ GitHub Copilot
- ✅ Cursor AI
- ✅ 其他支持 `.editorconfig` 的编辑器

## 自定义

如果需要个人定制，可以创建 `.claude/settings.local.json`（已在 `.gitignore` 中排除），该文件会覆盖项目设置而不影响团队配置。

---

**总结**：这些配置文件确保 AI 编程助手能够安全、一致、高效地帮助您开发 OpenClaw 项目。
